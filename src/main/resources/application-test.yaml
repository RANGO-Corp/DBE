#database configs
spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password:
    url: "jdbc:h2:mem:testedb"
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: "/h2-console"